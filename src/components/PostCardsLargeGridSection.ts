import styled from 'styled-components';

import { pagePadding, pageWidth } from '../styles';

const PostCardsLargeGridSection = styled.article`
  ${pageWidth};
  ${pagePadding};
  margin-top: -20px;
  position: relative;
  border-radius: 5px;
`;

export default PostCardsLargeGridSection;
